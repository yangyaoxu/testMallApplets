<!--components/w-tab-control/w-tab-control.wxml-->
<view class="w-tab-control">
<block wx:for="{{titles}}" wx:key="index">
    <view class="w-tab-control-title {{currentIndex === index ? 'action' : ''}}"
    bind:tap="handleSwitchControl" data-index="{{index}}">{{item}}</view>
</block>
</view>

